/**
 * @earlysteps/comparison-engine — the Comparison Section's deterministic logic (CLAUDE.md
 * §13/§14). A THIRD, standalone computation over Assessment A's and Assessment B's already-
 * produced output; never part of either engine (rule 7, §2).
 */
export * from './bands.js';
export * from './compareAssessments.js';
