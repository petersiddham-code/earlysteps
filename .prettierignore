node_modules/
dist/
coverage/
pnpm-lock.yaml

# Markdown is prose/clinical/verbatim content (product plan, CLAUDE.md, guardrail + prompt
# templates, clinical-review notes). Formatting is governed by clinical review, not Prettier,
# so a reformatter never silently reflows a safety-critical sentence or question wording.
*.md
