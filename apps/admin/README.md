# @earlysteps/admin (placeholder)

Web dashboard for the content team to manage question banks, activity libraries,
translations, and red-flag rule tuning without an app release (CLAUDE.md §4, product plan §6).
**Not implemented in the foundation phase.**

It edits the versioned data in `packages/content`; every change routes through the clinical
content-review gate (CLAUDE.md §9, `docs/clinical-review/`).
