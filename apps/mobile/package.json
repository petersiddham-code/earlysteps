{
  "name": "@earlysteps/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "React Native (Expo) app — placeholder. Not implemented in the foundation phase.",
  "scripts": {
    "start": "echo 'apps/mobile is a placeholder — not scaffolded yet (see docs/product-plan.md §5).' && exit 0"
  }
}
